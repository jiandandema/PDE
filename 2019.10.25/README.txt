将nu变为关于y的函数，此次实验试验了两种情况，第一种是W=1，第二种是W=0.1。迭代详情在每一个文件夹中log_name.log.log文件中。


W=1，迭代了55001步与精确解的二范数误差稳定在0.0182032。


W=0.1，达到停机条件maxtime=200退出迭代，此时宏观流体速度仍然有负值，但是延长模拟时间应该会变成正的。需要说明的是，我们为了保证nu的数量级的极大值不超过0.1，令Re=10000，但是没有相应的调小时间步长，这导致relaxtime在某些地方小于Δt/2。但是这貌似并没有特别影响迭代结果。